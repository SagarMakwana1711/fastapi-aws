version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACE_ME"
        LoadBalancerInfo:
          ContainerName: "fastapi-container"
          ContainerPort: 8080
